services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: appadmin
      POSTGRES_PASSWORD: app_secret
      POSTGRES_DB: expense_tracker_db
    volumes:
      - dbdata:/var/lib/postgresql/data
    ports:
      - "${DB_PORT}:5432"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U appadmin -d expense_tracker_db"]
      interval: 10s
      timeout: 5s
      retries: 5
  backend:
    build:
      context: ./backend
    image: expense-tracker-backend
    environment:
      DATABASE_URL: postgresql://appadmin:app_secret@db:5432/expense_tracker_db
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "${BACKEND_PORT}:8000"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request,sys;sys.exit(0 if urllib.request.urlopen('http://localhost:8000/health').status==200 else 1)"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
  frontend:
    build:
      context: .
      dockerfile: frontend.Dockerfile
    image: expense-tracker-frontend
    ports:
      - "${FRONTEND_PORT}:80"
    depends_on:
      - backend
    restart: unless-stopped
volumes:
  dbdata:
